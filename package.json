{
  "name": "marketplace-observability-probe",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "probe": "tsx src/checkout_probe.ts",
    "test": "tsx --test test/retry.test.ts",
    "check": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  }
}
